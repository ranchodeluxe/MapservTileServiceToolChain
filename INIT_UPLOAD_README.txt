
bal setup:

 Download and install Git
  git config --global user.name "Your Name"
  git config --global user.email gregcorradini@gmail.com
        

Next steps:

  mkdir MapservTileServiceToolChain
  cd MapservTileServiceToolChain
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:thebigspoon/MapservTileServiceToolChain.git
  git push origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:thebigspoon/MapservTileServiceToolChain.git
  git push origin master
      

Importing a Subversion Repo?

  Click here
      

When you're done:

  Continue


